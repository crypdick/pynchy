# God Mode Container

A container that can self-author pynchy and restart the service.

## Context

The goal is for this container to allow adding new features to pynchy (modify its codebase), have it restart itself, and after restart resume with context so it can continue working on the feature where it left off.

Should be accessible over WhatsApp in a special channel.

## Plan
TBD
