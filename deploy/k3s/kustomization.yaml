apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - rbac.yaml
  - postgres.yaml
  - temporal.yaml
  - pynchy.yaml
  - network-policy.yaml
  - release-monitor.yaml
configMapGenerator:
  - name: pynchy-release-monitor
    namespace: pynchy
    files:
      - release-monitor.sh
