# ============================================================================
# Pynchy Secrets (.env)
# ============================================================================
# Copy this file to .env in the project root and fill in your values:
#   cp config-examples/.env.EXAMPLE .env
#
# Non-secret config stays in config.toml. This file is for API keys, tokens,
# and passwords only. Uses __ as the nested delimiter to match config sections.
#
# .env is already in .gitignore — never commit it.
# ============================================================================

# ─── API Credentials ────────────────────────────────────────────────────────
# Claude OAuth tokens are auto-discovered from ~/.claude/.credentials.json.
# Only set anthropic_api_key if you use a pay-as-you-go API key instead.

# SECRETS__ANTHROPIC_API_KEY=sk-ant-api03-...
# SECRETS__OPENAI_API_KEY=sk-proj-...
# SECRETS__GH_TOKEN=ghp_...

# ─── Gateway (LiteLLM) ─────────────────────────────────────────────────────
# Env vars referenced by os.environ/ in litellm_config.yaml are auto-forwarded
# into the LiteLLM container at startup. Add keys here, reference in YAML.

# PYNCHY_ANTHROPIC_TOKEN=sk-ant-...
# PYNCHY_ANTHROPIC_TOKEN_SECONDARY=sk-ant-...  # uncomment for load balancing

# GATEWAY__MASTER_KEY=your-master-key
# GATEWAY__UI_PASSWORD=your-secure-password

# ─── Slack ──────────────────────────────────────────────────────────────────

# SLACK__BOT_TOKEN=xoxb-...
# SLACK__APP_TOKEN=xapp-...
