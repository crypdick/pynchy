# ============================================================================
# Pynchy Secrets (.env)
# ============================================================================
# Copy this file to .env in the project root and fill in your values:
#   cp config-examples/.env.EXAMPLE .env
#
# Non-secret config stays in config.toml. This file is for API keys, tokens,
# and passwords only. Uses __ as the nested delimiter to match config sections.
#
# .env is already in .gitignore — never commit it.
# ============================================================================

# ─── API Credentials ────────────────────────────────────────────────────────
# Only set anthropic_api_key if you use a pay-as-you-go API key instead of
# Claude Code OAuth (see `claude setup-token`).

# SECRETS__ANTHROPIC_API_KEY=sk-ant-api03-...
# SECRETS__OPENAI_API_KEY=sk-proj-...
# SECRETS__GH_TOKEN=ghp_...

# ─── Gateway (LiteLLM) ─────────────────────────────────────────────────────
# Env vars referenced by os.environ/ in litellm_config.yaml are auto-forwarded
# into the LiteLLM container at startup. Add keys here, reference in YAML.

# Name vars after the account they belong to, e.g.:
# ANTHROPIC_TOKEN_EMPLOYEE1=sk-ant-...
# ANTHROPIC_TOKEN_EMPLOYEE2=sk-ant-...   # uncomment for load balancing
# OPENAI_KEY_EMPLOYEE1=sk-proj-...

# OAuth token from `claude setup-token` (Pro/Max subscribers).
# LiteLLM auto-detects the sk-ant-oat* prefix and handles auth headers.
# CLAUDE_OAUTH_TOKEN=sk-ant-oat01-...

# GATEWAY__MASTER_KEY=your-master-key
# GATEWAY__UI_PASSWORD=your-secure-password

# ─── Slack Channel Plugin ────────────────────────────────────────────────────

# SLACK__BOT_TOKEN=xoxb-...
# SLACK__APP_TOKEN=xapp-...

# ─── Slack MCP Server ───────────────────────────────────────────────────────
# Browser tokens for korotovsky/slack-mcp-server (read-only Slack access).
# Extract xoxc/xoxd from browser cookies — see:
# https://github.com/korotovsky/slack-mcp-server/blob/master/docs/01-authentication-setup.md

# SLACK_MCP_XOXC_TOKEN=xoxc-...
# SLACK_MCP_XOXD_TOKEN=xoxd-...
